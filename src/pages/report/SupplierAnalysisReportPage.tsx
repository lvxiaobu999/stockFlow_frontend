import { routeMeta } from '@/config/routes'
import PlaceholderPage from '@/pages/PlaceholderPage'

/** 供应商采购分析页面（占位）：后续业务逻辑放在 features/report。 */
export default function SupplierAnalysisReportPage() {
  return <PlaceholderPage {...routeMeta.reportSupplierAnalysis} />
}
