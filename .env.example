# Browser-safe variables must use the VITE_ prefix.
VITE_HOST=localhost
VITE_PORT=5173
VITE_PREVIEW_PORT=4173
VITE_APP_NAME=StockFlow
# 本地无后端时使用 mock；接入后端时改为 false
VITE_USE_MOCK_API=true
# VITE_API_BASE_URL=http://localhost:3000/api
